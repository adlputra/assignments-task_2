function isOdd(n: number): boolean {
    return n % 2 !== 0; // Returns true if n is odd, false if n is even
  }
  
  // example
  console.log(isOdd(1001));  // true (1001 is odd)
  console.log(isOdd(1000)); // false (1000 is even)
  